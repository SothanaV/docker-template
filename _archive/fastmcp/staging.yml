services:
  mcp:
    container_name: ${PROJECT_NAME}-mcp
    build: ./server
    command: bash run-server.sh 4
    volumes:
      - ./server:/backend
    ports:
      - 8080:8000
    env_file:
      - .env