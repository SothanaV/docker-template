[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend template"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
    "python-dotenv>=1,<2",
]

[tool.uv]
package = false
