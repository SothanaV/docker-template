services:
  mcp:
    container_name: ${PROJECT_NAME}-mcp
    build: ./server
    command: bash run-server-dev.sh
    volumes:
      - ./server:/backend
    ports:
      - 8080:8000
    env_file:
      - .env