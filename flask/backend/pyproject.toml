[project]
name = "backend"
version = "0.1.0"
description = "Flask backend template"
requires-python = ">=3.14"
dependencies = [
    "flask>=3,<4",
    "gunicorn>=23,<24",
]

[tool.uv]
package = false
